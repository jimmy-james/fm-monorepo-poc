import { Toolbar } from '../../interfaces/Toolbar';
import * as ActionType from '../actionTypes/toolbarActionTypes';
import { ToolbarActions } from '../actions/toolbarActions';
import { initialAppState } from './initialState';

// export const initialState: Toolbar = initialAppState.toolbarState;

// export default function toolbarAppReducer(state = initialState, action: ToolbarActions): Toolbar {
//   switch (action.type) {
//     case ActionType.LOAD_TOOL_SETTINGS_SUCCESS:
//       return state;
//     default:
//       return state;
//   }
// }
